class flocking():
    def __init__(self,agents):
        self.agents=agents
    def computeAlignaments():
        velocity
        for i in self.agents:
            v=[0,0]
            neighborCount=0
            for j in self.agents:
                if(i!=j and distance(i,j)<100):
                    v[0]+=j.velocity.x
                    v[j]+=j.velocity.y
                    neighborCount+=1
        